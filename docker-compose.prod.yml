services:
  teamb-app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "3000:3000" 
      
  nginx:
    build: ./nginx
    restart: always
    ports:
      - '80:80'
    depends_on:
      - teamb-app
